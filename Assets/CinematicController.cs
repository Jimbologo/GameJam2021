using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CinematicController : MonoBehaviour
{
    //We are going to want to have a person laying down, camera starts zoomed in on the eyeball.
    //We can do a wierd anim of the eyeball rotating
    //Cam zooms out more to see full face, we then have him smile (thumbs up also maybe?)
    //Finally we then smooth transition to the gameplay camera, and everything starts
}
